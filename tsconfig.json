{
  "compilerOptions": {
    "jsx": "preserve", // JSX 설정
    "module": "esnext", // ES 모듈 사용
    "target": "esnext", // 최신 JavaScript 문법 사용
    "baseUrl": ".", // 기본 경로 설정
    "moduleResolution": "node", // 모듈 해석 방식 설정
    "allowJs": true, // JavaScript 파일 허용
    "checkJs": false // JS 파일에 대해 타입 체크를 하지 않음
    ,
    "lib": [
      "dom",
      "dom.iterable",
      "esnext"
    ],
    "skipLibCheck": true,
    "strict": false,
    "noEmit": true,
    "incremental": true,
    "esModuleInterop": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "plugins": [
      {
        "name": "next"
      }
    ]
  },
  "include": [
    "src/**/*" // src 디렉토리 하위 모든 파일 포함
    ,
    ".next/types/**/*.ts"
  ],
  "exclude": [
    "node_modules" // node_modules 폴더는 제외
  ]
}
